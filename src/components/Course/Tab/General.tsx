import TabLayout from '@/layouts/TabLayout';

const General = () => {
	return <TabLayout header={<div>background</div>} leftBody={<div>code</div>} rightBody={<div>text</div>} />;
};

export default General;
